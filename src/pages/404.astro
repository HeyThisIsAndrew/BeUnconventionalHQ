---
import Layout from '../layouts/Layout.astro';
import PageTitle from '../components/PageTitle.astro';
import Button from '../components/Button.astro';
import { site } from '../data/site.js';

const title = `Page Not Found | ${site.name}`;
const description =
  'That page went off-script. Head back to Be Unconventional HQ.';
---

<Layout title={title} description={description}>
  <main class="notfound-page">
    <div class="notfound-inner">
      <span class="notfound-code" aria-hidden="true">404</span>
      <PageTitle as="h1" kicker="Off The Map" primary="Page" secondary="Not Found" />
      <p class="notfound-text">
        This one went off-script. The page you're after moved, vanished, or
        never existed &mdash; but the good stuff is still one click away.
      </p>
      <div class="notfound-actions">
        <Button href="/">Back to Home</Button>
        <a class="notfound-link" href="/feed">Browse the Feed &rarr;</a>
      </div>
    </div>
  </main>
</Layout>

<style>
  .notfound-page {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 6rem 1.5rem;
  }

  .notfound-inner {
    max-width: 640px;
  }

  .notfound-code {
    display: block;
    font-family: var(--font-display);
    font-size: clamp(4rem, 18vw, 9rem);
    font-weight: 800;
    line-height: 1;
    color: var(--color-accent);
    letter-spacing: 0.05em;
    margin-bottom: 1rem;
  }

  .notfound-text {
    font-family: var(--font-body);
    color: var(--color-white-muted);
    font-size: 1.05rem;
    line-height: 1.6;
    margin: 1.5rem auto 2.5rem;
    max-width: 32rem;
  }

  .notfound-actions {
    display: flex;
    gap: 1.5rem;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
  }

  .notfound-link {
    color: var(--color-white);
    text-decoration: none;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--color-accent);
    padding-bottom: 2px;
    transition: color 0.3s ease;
  }

  .notfound-link:hover {
    color: var(--color-accent);
  }
</style>
