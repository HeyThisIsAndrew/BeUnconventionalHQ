---
interface Props {
  gap?: string;
  minColWidth?: string;
}
const { gap = '2.5rem', minColWidth = 'min(100%, 400px)' } = Astro.props;
---
<div style={`display: grid; grid-template-columns: repeat(auto-fill, minmax(${minColWidth}, 1fr)); gap: ${gap};`}>
  <slot />
</div>
