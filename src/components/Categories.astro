---
import { categories } from '../data/categories.js';
---

<section class="categories-section">
  <div class="section-header animate-on-scroll">
    <h2 class="hero-title-stack">
      <span class="word-be">Explore The HQ</span>
      <span class="word-unconventional">What We</span>
      <span class="word-hq">Cover</span>
    </h2>
  </div>

  <div class="categories-grid">
    {
      categories.map((cat, index) => (
        <a
          href={'/category/' + cat.slug}
          class="category-card animate-on-scroll"
          data-delay={index * 20}
        >
          <div class="category-header">
            <div class="category-label">{cat.label}</div>
            <img class="category-icon" src={cat.image} alt={cat.label} />
          </div>
          <h3 class="category-title">{cat.label}</h3>
          <p class="category-description">{cat.blurb}</p>
          <div class="action-link">Explore →</div>
        </a>
      ))
    }
  </div>
</section>
