---
interface Props {
  className?: string;
  style?: string;
}
const { className = '', style = '' } = Astro.props;
---
<div class={`section-header reveal animate-on-scroll ${className}`} style={style}>
  <slot />
</div>
