---
interface Props {
  pt?: string;
  pb?: string;
  maxWidth?: string;
}
const { pt = '4rem', pb = '6rem', maxWidth = '1536px' } = Astro.props;
---
<section class="container" style={`padding-top: ${pt}; padding-bottom: ${pb}; max-width: ${maxWidth}; margin: 0 auto;`}>
  <slot />
</section>
