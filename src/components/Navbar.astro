---
const base = import.meta.env.BASE_URL;
---

<header id="navbar">
  <a href={`${base}`} class="nav-logo">
    <img src={`${base}logo.png`} alt="Be Unconventional HQ" />
  </a>
  <nav>
    <ul>
      <li><a href={`${base}videos`}>Videos</a></li>
      <li><a href={`${base}articles`}>Articles</a></li>
      <li><a href={`${base}about`}>About</a></li>
      <li><a href={`${base}contact`} class="cta">Contact</a></li>
    </ul>
  </nav>
</header>

<script>
  const navbar = document.getElementById('navbar');
  if (navbar) {
    window.addEventListener('scroll', () => {
      if (window.scrollY > 60) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
    });
  }
</script>

<style>
  /* Styles live in global.css */
</style>

<!-- <style is:global>
  #navbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem 2.5rem;
    background: rgba(10, 10, 10, 0.6);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.4);
    transition: background 0.5s ease, box-shadow 0.5s ease;
  }

  #navbar.scrolled {
    background: rgba(10, 10, 10, 0.92);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.6);
  }

  #navbar:hover {
    background: rgba(20, 20, 20, 0.75);
    box-shadow: 0 4px 40px rgba(204, 0, 0, 0.08);
  }

  #navbar.scrolled:hover {
    background: rgba(25, 25, 25, 0.95);
    box-shadow: 0 4px 40px rgba(204, 0, 0, 0.12);
  }

  .nav-logo img {
    height: 44px;
    width: auto;
    display: block;
    transition: opacity 0.3s ease;
  }

  .nav-logo:hover img {
    opacity: 0.8;
  }

  #navbar nav ul {
    list-style: none;
    display: flex;
    align-items: center;
    gap: 2.5rem;
    margin: 0;
    padding: 0;
  }

  #navbar nav ul li a {
    color: rgba(245, 245, 245, 0.8);
    font-family: var(--font-body);
    font-size: 0.8rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    text-decoration: none;
    position: relative;
    padding-bottom: 4px;
    transition: color 0.3s ease, text-shadow 0.3s ease;
  }

  #navbar nav ul li a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-accent);
    transition: width 0.35s cubic-bezier(0.25, 0.8, 0.2, 1);
  }

  #navbar nav ul li a:hover {
    color: #cc0000 !important;
    text-shadow: 0 0 16px rgba(204, 0, 0, 0.6);
  }

  #navbar nav ul li a:hover::after {
    width: 100%;
  }

  #navbar nav ul li a.cta {
    color: var(--color-accent);
  }

  #navbar nav ul li a.cta:hover {
    color: #ff1a1a !important;
    text-shadow: 0 0 20px rgba(204, 0, 0, 0.8);
  }

  @media (max-width: 768px) {
    #navbar {
      padding: 1rem 1.25rem;
    }

    #navbar nav ul {
      gap: 1.25rem;
    }

    #navbar nav ul li a {
      font-size: 0.7rem;
    }
  }
</style> -->
