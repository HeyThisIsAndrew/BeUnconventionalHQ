---
import { getArticles } from '../pages/articles.astro';
const base = import.meta.env.BASE_URL;
const allArticles = await getArticles();
const latestArticles = allArticles.slice(0, 3);
---

<div class="latest-3d-root">
  <div class="latest-scroll-region">
    <div class="latest-sticky-viewport">
      <section class="latest-section">
        <div class="section-header animate-on-scroll">
          <h2 class="hero-title-stack">
            <span class="word-be">From</span>
            <span class="word-unconventional">The Feed</span>
            <span class="word-hq">Latest</span>
          </h2>
        </div>

        <div class="articles-grid">
          {
            latestArticles.map((article, index) => (
              <article
                class="article-card animate-on-scroll"
                data-delay={index * 100}
              >
                <div class="article-image-wrapper">
                  <img
                    src={article.image}
                    alt={article.title}
                    class="article-image"
                  />
                </div>
                <div class="article-content">
                  <span class="article-date">{article.date}</span>
                  <h2 class="article-title">{article.title}</h2>
                  <p class="article-excerpt">{article.excerpt}</p>
                  <a
                    href={article.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="article-link"
                  >
                    Read Full Article
                  </a>
                </div>
              </article>
            ))
          }
        </div>

        <div class="latest-view-all animate-on-scroll">
          <a href={`${base}feed`} class="view-all-link">
            View All Content →
          </a>
        </div>
      </section>
    </div>
  </div>
</div>
