# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# notes and document files
docs/

# backup files
backups/

# antigravity skills
.antigravity
scripts/anti-gravity/.wrangler

# wrangler local dev state
.wrangler/
.wrangler-logs/

# Lighthouse CI gate reports (scripts/lighthouse-check.mjs)
.lighthouse-reports/

# Ad-hoc Lighthouse scratch at the repo root. These have been committed once
# (1.2 MB of JSON and seven throwaway runners) and cleaned up twice, so the
# pattern is here rather than in anyone's memory.
/lh*.mjs
/lh*.js
/lh-report.json
/lh-local.json

# Local CMS image uploads (dev-only, public/api/local-cms/upload) - written
# straight to public/, must never be committed or it ships to production.
public/uploads/
analytics-setup-instructions.md

# Visual parity captures (scripts/visual-parity.mjs)
.visual-parity/
.lighthouse-reports-desktop/
